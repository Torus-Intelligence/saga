interface:
  display_name: "Saga Core"
  short_description: "Author and run Saga workflow tests"
  default_prompt: "Use $saga-core to add a Saga fixture for this workflow and verify it against the existing code."
